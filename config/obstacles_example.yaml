# Obstacle Configuration for GF Guidance
# Position is in robot base frame coordinates (meters)
# Radius is in meters

obstacles:
  # Obstacle blocking direct path to goal
  - pos: [0.4, -0.1, 0.5]
    radius: 0.15

  # Additional obstacles (uncomment to use)
  # - pos: [0.3, 0.2, 0.4]
  #   radius: 0.1
  # - pos: [0.5, -0.2, 0.6]
  #   radius: 0.12
